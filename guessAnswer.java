public class CompMode
{

   public int[] guessAnswer(whitenum, rednum)
   {
      int[] guess = new int[4];
      for(int i = 0; i < guess.length; i++)
      {
         guess[i] = (int Math.random()*1+6);
      }
      
      
      if all red end
      
      if all white keep same colors organize differently
      
      if 2 white pick two from array generate 2 new colors
      
      if no pegs generate new random color without those pegs
      
      int[] a =
      return a;
   }
   
   

 
      
